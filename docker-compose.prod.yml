version: "3"
services:
  proxy:
    volumes:
      - ./proxy/nginx.prod.conf:/etc/nginx/nginx.conf
      - ./proxy/nginx.prod.conf:/etc/nginx/conf.d/default.conf
  server1:
    environment:
      NODE_ENV: production
  server2:
    environment:
      NODE_ENV: production
