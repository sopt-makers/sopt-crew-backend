# 🛠️ Spring Boot + Lambda 개발 워크플로우 가이드

효율적인 개발을 위해 아래 **3단계 워크플로우**를 권장합니다.
비즈니스 로직 개발은 빠르게 진행하고, 배포 전에만 Lambda 환경과 맞추는 방식입니다.

---

## 1단계: 기능 구현 & 빠른 테스트 (Standard Mode)

**"Lambda라고 해서 매번 SAM으로 띄울 필요는 없습니다."**
비즈니스 로직은 일반 Spring Boot와 100% 동일하므로, 평소처럼 익숙한 방식으로 개발하세요.

* **실행 방법**: `MainApplication`을 IDE에서 바로 실행하거나, 터미널에서 아래 명령어를 사용합니다.

```bash
# 로컬 개발 프로필로 실행
cd main && ./gradlew bootRun --args='--spring.profiles.active=dev'
```

> **💡 장점:**
> * 서버 재시작이 매우 빠릅니다 (Hot Reloading).
> * IDE의 디버거(Breakpoints)를 편하게 사용할 수 있습니다.

---

## 2단계: Lambda 호환성 검증 (Local SAM)

기능 구현이 끝났다면, **"이 코드가 실제 Lambda 환경에서도 잘 돌까?"**를 확인하는 단계입니다.
(로컬에서는 잘 되는데 Lambda 환경(OS, 라이브러리 등)에서만 발생하는 이슈를 사전에 방지합니다.)

* **실행 방법**: 터미널을 열고 아래 순서대로 실행하면 **로컬에 가상의 AWS Lambda 서버(localhost:3000)**가 뜹니다.

```bash
# 1. Lambda용 JAR 빌드 (필수)
cd main && ./gradlew clean lambdaJar

# 2. 로컬 API Gateway 실행
cd ../lambda
sam local start-api --template template-dev.yaml --config-env dev
```

* **테스트**: 이제 `http://localhost:3000`으로 API 요청을 보내보세요.
* **특징**: 실제 AWS Lambda + API Gateway와 거의 동일한 환경(Docker)에서 실행됩니다.

---

## 3단계: Dev 환경 배포 (Deployment)

로컬 검증이 끝났다면, 실제 AWS Dev 환경에 배포하여 통합 테스트를 진행합니다.

### 방법 A: 수동 배포 (스크립트 실행)

로컬에서 즉시 배포하고 싶을 때 사용합니다.

```bash
cd lambda
./deploy-dev.sh
```

### 방법 B: 자동 배포 (GitHub Actions)

코드를 원격 저장소에 올리면 CI/CD 파이프라인이 자동으로 배포합니다.

```bash
git push origin feature/my-new-feature
```

> **Note:** PR을 올리고 `develop` 브랜치에 머지되면 자동으로 Dev 서버에 배포됩니다.
