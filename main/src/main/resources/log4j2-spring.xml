<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="CONSOLE_LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight{%-5level}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=green, DEBUG=green bold, TRACE=blue} %style{[%thread]}{cyan} %style{[%X{traceId}]}{green} %style{[%X{userId}]}{magenta} %style{[%X{clientIp}]}{blue} %style{[%X{requestInfo}]}{yellow} %style{%logger{36}}{bright,white} - %msg%n</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_LOG_PATTERN}" disableAnsi="false"/>
        </Console>

        <Sentry name="Sentry" dsn="${spring:sentry.dsn}"/>
    </Appenders>

    <Loggers>
        <!-- Spike apply 경로 전용: 비동기 로깅 (Disruptor 기반 lock-free) -->
        <AsyncLogger name="crew.spike.apply" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="Sentry"/>
        </AsyncLogger>

        <Logger name="org.sopt.makers.crew" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="Sentry"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="Sentry"/>
        </Root>
    </Loggers>
</Configuration>
